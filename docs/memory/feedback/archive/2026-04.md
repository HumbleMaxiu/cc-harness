# Feedback Archive 2026-04

> 模板：归档已经完成最终汇总的反馈模式和趋势，不复制原始 lint / test 输出。

## 月度概览

- high_frequency_patterns: `docs 镜像未同步`、`反馈不应记录原始 lint 文本`
- promoted_rules: `feedback 先抽象为 pattern/rule 再入 memory`
- unresolved_cross_task_risks: `运行层权限模式仍需补官方配置矩阵`

## 代表性问题模式

| pattern | rule | action_type | risk_level | occurrence_count | status |
|---------|------|-------------|------------|------------------|--------|
| `feedback 直接记录一次性错误文本` | `进入 memory 前先提炼为可复用规则；原始输出保留在 evidence` | `workflow_rule` | `medium` | `2` | `rolled_up` |

## 升级到 Prevents-Recurrence

| pattern | prevention | synced_locations | status |
|---------|------------|------------------|--------|
| `feedback 直接记录一次性错误文本` | `feedback memory 只保留 pattern/rule，原始 lint/test 输出不进入长期 memory` | `docs/feedback/feedback-collection.md`, `docs/memory/feedback/agent-feedback.md`, `skills/dev-workflow/SKILL.md` | `candidate` |

## 固定输出格式

每月归档建议按以下顺序输出：

1. `月度概览`
2. `代表性问题模式`
3. `升级到 Prevents-Recurrence`
4. `备注`

每条归档记录至少包含：

- `pattern`
- `rule`
- `action_type`
- `risk_level`
- `occurrence_count`
- `status`

## 备注

- 本文件只保留摘要和趋势。
- 原始交接链路、命令输出和逐条 evidence 仍应回到对应任务交接文档中查看。
