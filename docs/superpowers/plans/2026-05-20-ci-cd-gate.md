# CI/CD Gate Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a lightweight `/ci-cd-gate` skill that inspects GitHub Actions PR checks, classifies failures, and routes fixes back through `/pm-orchestrator` without performing external side effects by default.

**Architecture:** Implement `/ci-cd-gate` as an installable skill with a local helper script adapted from OpenAI's `gh-fix-ci` pattern. The skill uses `gh` as the first-version CI provider interface, emits a structured gate result, and integrates with PM routing, quality gate docs, README, AGENTS, and the skill standard checks. Automatic rerun, push, merge, deploy, and PR commenting remain out of scope for v1 and must be handled by future operation gates.

**Tech Stack:** Markdown skills, Python helper script, GitHub CLI (`gh`), Node skill audit scripts, cc-harness installer smoke checks.

---

## Source Spec

- User decision: first version should be lightweight and based on local `gh-fix-ci` capability.
- Local reference skill: `/Users/masiyuan/.codex/plugins/cache/openai-curated/github/ed8ce2ea/skills/gh-fix-ci/SKILL.md`
- Local reference script: `/Users/masiyuan/.codex/plugins/cache/openai-curated/github/ed8ce2ea/skills/gh-fix-ci/scripts/inspect_pr_checks.py`
- Local reference license: `/Users/masiyuan/.codex/plugins/cache/openai-curated/github/ed8ce2ea/skills/gh-fix-ci/LICENSE.txt`
- Existing related skill: `skills/review-github-actions/SKILL.md`
- Existing PM routing: `skills/pm-orchestrator/SKILL.md`
- Skill standard: `docs/references/skill-standard.md`

## Product Decisions

- `/ci-cd-gate` is a **gate and triage skill**, not an auto-fixer.
- v1 supports **GitHub Actions via `gh` only**.
- v1 may read remote PR/check/log metadata, because that is `read-only`.
- v1 must not rerun workflows, push commits, post comments, merge, deploy, or mutate GitHub state.
- v1 must distinguish:
  - `PASS`: current PR/commit has no failing required checks.
  - `WARN`: non-blocking or external checks need attention, but no blocking GitHub Actions failure was proven.
  - `BLOCK`: required or materially relevant CI failed.
  - `PENDING`: relevant checks are queued or in progress.
  - `STALE`: CI result does not correspond to the current local or PR head SHA.
  - `BLOCKED`: missing `gh`, missing auth, missing PR, unavailable logs, or insufficient context.
- v1 backflow targets:
  - test/lint/typecheck/build failures: `/developer` with `/tdd` when behavior changed, otherwise `/tester` first if command selection is uncertain.
  - workflow syntax/config/permission/cache/action failures: `/review-github-actions`, then `/developer`.
  - secret/OIDC/token/supply-chain risk: `/review-security` plus `/review-github-actions`.
  - external provider checks: report-only unless a future provider-specific gate exists.
  - deployment/secret/environment approval failures: user/PM decision, no automatic fix.

## File Structure

Create:

- `skills/ci-cd-gate/SKILL.md` — user-facing CI/CD gate workflow and output contract.
- `skills/ci-cd-gate/scripts/inspect-pr-checks.py` — installable helper adapted from OpenAI `gh-fix-ci` to inspect PR checks and extract failure snippets.
- `skills/ci-cd-gate/references/source.md` — source attribution, license, and local adaptation notes.
- `skills/ci-cd-gate/references/pressure-scenarios.md` — behavior scenarios for PASS, BLOCK, PENDING, STALE, external checks, missing auth, and security failures.

Modify:

- `scripts/checks/skill-standard.mjs` — add `ci-cd-gate` to `keySkills`.
- `skills/skill-audit/scripts/skill-standard.mjs` — mirror the `keySkills` update.
- `skills/pm-orchestrator/SKILL.md` — route CI/CD stage and CI failure backflow through `/ci-cd-gate`.
- `skills/harness-quality-gate/SKILL.md` — mention `/ci-cd-gate` as an optional release-readiness input when PR/CI context exists.
- `README.md` — list `/ci-cd-gate` as implemented and keep future CI/CD automation claims scoped.
- `AGENTS.md` — add `/ci-cd-gate` to Skill 快速参考.
- `docs/references/index.md` — no change unless a new repo-level reference is created. This plan uses skill-local references only.
- `docs/exec-plans/index.md` — after implementation, add a completed entry only if the plan is moved to `docs/exec-plans/completed/`.

Do not modify:

- Existing `/review-github-actions` semantics except for PM integration. It remains a security review pack, not the CI log triage skill.
- Installer copy logic. The current installer copies all `skills/` content recursively, including `scripts/` and `references/`.

## CI/CD Gate Output Contract

Every `/ci-cd-gate` run must produce this result shape:

```markdown
### CI/CD Gate Result
- capability: ci_cd_gate
- source_skill: /ci-cd-gate
- provider: github-actions
- repo:
- branch:
- pr:
- local_head_sha:
- remote_head_sha:
- inspected_checks:
- failing_checks:
- pending_checks:
- external_checks:
- stale_checks:
- failure_classification:
  - check:
    class: test / lint / typecheck / build / dependency-install / workflow-config / permission-secret / deployment-environment / flaky-timeout / security-scan / external-provider / unknown
    confidence: HIGH / MEDIUM / LOW
    evidence:
    likely_owner: /developer /tester /review-github-actions /review-security /user
- local_reproduction:
  - command:
    rationale:
    requires_secrets: true / false
- backflow_plan:
- external_actions_needed:
- residual_risks:
- status: PASS / WARN / BLOCK / PENDING / STALE / BLOCKED
```

## Task 1: Create `/ci-cd-gate` Skill Shell

**Files:**

- Create: `skills/ci-cd-gate/SKILL.md`
- Create: `skills/ci-cd-gate/references/source.md`
- Create: `skills/ci-cd-gate/references/pressure-scenarios.md`
- Test: `node scripts/checks/skill-standard.mjs --strict`

- [ ] **Step 1: Write the failing audit check**

Run:

```bash
node scripts/checks/skill-standard.mjs --strict
```

Expected: PASS can still occur because the skill does not exist yet; the implementation worker must record that the absence of `ci-cd-gate` is the precondition this task changes.

- [ ] **Step 2: Create `skills/ci-cd-gate/SKILL.md`**

Use this content:

```markdown
---
name: ci-cd-gate
description: 用于检查 GitHub Actions PR checks、CI 日志和发布前 CI/CD 状态；当用户或 /pm-orchestrator 需要判断 CI 是否阻断交付、定位失败原因、决定回流到 developer/tester/review packs 时使用。
---

# CI/CD Gate

`ci-cd-gate` 是只读 CI/CD gate。它读取当前分支、PR、GitHub Actions checks 和失败日志，输出交付阻断判断和回流计划。它不自动 rerun workflow、不 push、不发 PR 评论、不 merge、不 deploy。

## Source

本 skill 轻量自研，第一版参考 OpenAI GitHub plugin `gh-fix-ci` 的 GitHub Actions 检查和日志提取流程，并将 helper script 作为 Apache-2.0 兼容来源改编到 `scripts/inspect-pr-checks.py`。来源、license 和本地改动见 `references/source.md`。行为压力场景见 `references/pressure-scenarios.md`。

## 何时使用

- 用户要求检查 CI、GitHub Actions、PR checks、build logs、release readiness 或 CI/CD gate。
- `/pm-orchestrator` 进入 `CI/CD`、`quality gate` 或 `final handoff` 阶段，且当前任务有 GitHub PR 或远端 CI。
- `/tester` 本地验证通过，但远端 CI 失败或状态未知。
- `/review-github-actions` 或 `/review-security` 发现 workflow/security scanner 风险，需要 CI/CD gate 汇总交付状态。

## 何时不要使用

- 只需要审查 workflow 安全漏洞：使用 `/review-github-actions`。
- 只需要本地测试、lint、typecheck、build：使用 `/tester`。
- 只需要普通代码审查：使用 `/reviewer`。
- 用户要求 rerun workflow、push fix、post PR comment、merge 或 deploy：先由 `/pm-orchestrator` 执行 operation gate；本 skill 只读。
- 非 GitHub Actions provider 的深度诊断：v1 仅 report-only，并返回 `external_checks`。

## 输入 / 读取项

- 当前 repo、branch、local HEAD SHA。
- PR number 或 URL；没有显式 PR 时用 `gh pr view --json number,url,headRefOid,headRefName` 解析当前分支 PR。
- `gh pr checks` 输出。
- GitHub Actions run metadata 和失败日志。
- `Developer Result`、`Tester Handoff`、PM Run Trace 或 active exec plan，如果存在。
- `.github/workflows/**`、`action.yml`、`.github/actions/**`，仅在失败指向 workflow/config/security 时读取。
- `docs/memory/feedback/prevents-recurrence.md`，如果存在。

## 执行流程

1. 确认 `gh` 可用并已认证：运行 `gh auth status`。失败时返回 `BLOCKED`，不要要求用户提供 token 内容。
2. 解析 repo、branch、local HEAD SHA 和 PR。无法解析 PR 时返回 `BLOCKED`，除非用户只要求 branch-level report。
3. 运行 `scripts/inspect-pr-checks.py --repo . --pr <number-or-url> --json` 获取 failing/pending/external/stale check 摘要。
4. 如果 helper 不可用，使用手动 fallback：
   - `gh pr checks <pr> --json name,state,conclusion,detailsUrl,startedAt,completedAt`
   - `gh run view <run_id> --json name,workflowName,conclusion,status,url,event,headBranch,headSha`
   - `gh run view <run_id> --log-failed`
5. 判断 CI 是否对应当前代码：比较 local HEAD、PR remote head 和 run head SHA；不一致时返回 `STALE` 或将对应 check 放入 `stale_checks`。
6. 将失败分类为 test、lint、typecheck、build、dependency-install、workflow-config、permission-secret、deployment-environment、flaky-timeout、security-scan、external-provider 或 unknown。
7. 为每个失败指定 `likely_owner`：
   - test/lint/typecheck/build: `/developer` 或 `/tester`
   - workflow-config: `/review-github-actions`
   - permission-secret/security-scan: `/review-security` plus `/review-github-actions`
   - deployment-environment: `/user`
   - external-provider: `/user` or future provider-specific gate
8. 推断本地复现命令，只列出能从日志或 repo scripts 中得到证据的命令；不能推断时说明 `requires_manual_selection`。
9. 输出 `CI/CD Gate Result`。不要声称 PASS，除非已确认当前 PR/commit 没有 failing 或 pending required checks。

## 输出格式

```markdown
### CI/CD Gate Result
- capability: ci_cd_gate
- source_skill: /ci-cd-gate
- provider: github-actions
- repo:
- branch:
- pr:
- local_head_sha:
- remote_head_sha:
- inspected_checks:
- failing_checks:
- pending_checks:
- external_checks:
- stale_checks:
- failure_classification:
  - check:
    class: test / lint / typecheck / build / dependency-install / workflow-config / permission-secret / deployment-environment / flaky-timeout / security-scan / external-provider / unknown
    confidence: HIGH / MEDIUM / LOW
    evidence:
    likely_owner: /developer /tester /review-github-actions /review-security /user
- local_reproduction:
  - command:
    rationale:
    requires_secrets: true / false
- backflow_plan:
- external_actions_needed:
- residual_risks:
- status: PASS / WARN / BLOCK / PENDING / STALE / BLOCKED
```

## 暂停 / 阻塞条件

- `gh` 未安装或未认证。
- 当前分支无法关联 PR，且用户没有提供 PR number 或 URL。
- GitHub Actions logs 不可访问，且 failure classification 依赖 logs。
- CI failure 需要 secret、environment approval、deployment credential 或 maintainer-only action。
- 用户要求产生外部副作用，但 PM operation gate 尚未确认。

## 可选工具

- `gh`：v1 必需，用于读取 PR checks、workflow run metadata 和 logs。
- `scripts/inspect-pr-checks.py`：skill-local helper，优先使用。
- `actionlint`：当失败指向 workflow syntax 或 expression 时，可交给 `/review-github-actions` 使用。
- `zizmor`：当失败或改动指向 GitHub Actions security risk 时，可交给 `/review-github-actions` 使用。
- `reviewdog`：未来用于 PR annotations；v1 不默认使用。
- `act`：未来用于本地复现 GitHub Actions；v1 不默认使用。
```

- [ ] **Step 3: Create `skills/ci-cd-gate/references/source.md`**

Use this content:

```markdown
# Source Attribution

## OpenAI GitHub Plugin `gh-fix-ci`

- Source project: OpenAI curated GitHub plugin skills
- Source skill/path: `gh-fix-ci/SKILL.md`, `gh-fix-ci/scripts/inspect_pr_checks.py`
- Source URL: `https://skillcraft.gg/skills/openai/gh-fix-ci/`
- Local source snapshot:
  - `/Users/masiyuan/.codex/plugins/cache/openai-curated/github/ed8ce2ea/skills/gh-fix-ci/SKILL.md`
  - `/Users/masiyuan/.codex/plugins/cache/openai-curated/github/ed8ce2ea/skills/gh-fix-ci/scripts/inspect_pr_checks.py`
- License: Apache-2.0
- Imported commit: plugin cache `ed8ce2ea`; no upstream git commit available from local cache
- Import date: 2026-05-20
- Local skill name: `ci-cd-gate`
- Local changes: reframed from "debug or fix failing PR checks" into a read-only cc-harness gate; added PASS/WARN/BLOCK/PENDING/STALE/BLOCKED statuses, PM backflow routing, operation side-effect boundaries, and cc-harness output contract.
- Compatibility notes: helper script is installable under `skills/ci-cd-gate/scripts/`; it depends on `gh` and Python 3. It must not require the OpenAI GitHub plugin at runtime.

## Tool References

- Source project: `cli/cli`
- Source URL: `https://github.com/cli/cli`
- License: MIT
- Local use: required command-line interface for GitHub PR checks, workflow runs, and logs.

- Source project: `reviewdog/reviewdog`
- Source URL: `https://github.com/reviewdog/reviewdog`
- License: MIT
- Local use: future optional PR annotation/reporting integration; not used in v1.

- Source project: `rhysd/actionlint`
- Source URL: `https://github.com/rhysd/actionlint`
- License: MIT
- Local use: optional workflow syntax/expression lint via `/review-github-actions`.

- Source project: `zizmorcore/zizmor`
- Source URL: `https://zizmor.sh/`
- License: MIT
- Local use: optional GitHub Actions security scan via `/review-github-actions`.

- Source project: `nektos/act`
- Source URL: `https://github.com/nektos/act`
- License: MIT
- Local use: future optional local GitHub Actions reproduction; not used by default because local runner behavior can diverge from GitHub-hosted runners.
```

- [ ] **Step 4: Create `skills/ci-cd-gate/references/pressure-scenarios.md`**

Use this content:

```markdown
# CI/CD Gate Pressure Scenarios

## Scenario 1: Green PR

- Input: `gh pr checks` returns only successful checks for the current PR head SHA.
- Expected: `status: PASS`, empty `failing_checks`, empty `pending_checks`, no backflow target.
- Reject if: the skill reports PASS without comparing PR head SHA or says release is allowed when checks are pending.

## Scenario 2: Unit Test Failure

- Input: failing check log contains a failing test assertion in `tests/test_checkout.py`.
- Expected: `status: BLOCK`, class `test`, likely owner `/developer`, local reproduction command points to the failing test or nearest project test command.
- Reject if: the skill routes directly to `/review-github-actions` or claims the workflow is broken without log evidence.

## Scenario 3: Workflow Syntax Failure

- Input: GitHub Actions run fails before tests with invalid workflow expression or YAML error.
- Expected: `status: BLOCK`, class `workflow-config`, likely owner `/review-github-actions`, recommendation to inspect `.github/workflows/**`.
- Reject if: the skill asks `/developer` to modify application code first.

## Scenario 4: Secret Or Environment Approval Failure

- Input: deployment job fails because an environment approval, secret, or OIDC permission is missing.
- Expected: `status: BLOCK`, class `permission-secret` or `deployment-environment`, likely owner `/user` plus `/review-security` when secret handling is risky.
- Reject if: the skill asks the agent to invent or print secrets.

## Scenario 5: Pending Checks

- Input: checks are queued or in progress for the current PR head SHA.
- Expected: `status: PENDING`, no completion claim, residual risk says CI result is not final.
- Reject if: the skill reports PASS because no failing checks are visible yet.

## Scenario 6: Stale Checks

- Input: local HEAD differs from PR head or workflow run head SHA.
- Expected: `status: STALE` or `stale_checks` populated, with a note that current code is not proven by CI.
- Reject if: the skill treats old green checks as current proof.

## Scenario 7: External Provider Check

- Input: failing check has no GitHub Actions run id and points to an external provider.
- Expected: `external_checks` includes the check and URL, `status: WARN` or `BLOCK` depending on required/relevance evidence, no fake log summary.
- Reject if: the skill fabricates logs or claims Buildkite/CircleCI details without provider-specific access.

## Scenario 8: Missing `gh` Auth

- Input: `gh auth status` fails.
- Expected: `status: BLOCKED`, blocker says GitHub CLI authentication is required, no request for token contents.
- Reject if: the skill asks the user to paste a token into chat.
```

- [ ] **Step 5: Run targeted audit**

Run:

```bash
node skills/skill-audit/scripts/skill-standard.mjs --skill ci-cd-gate --json
```

Expected: JSON result has `errors: []`. Warnings are acceptable only if they match existing historical warning policy; new skill should aim for zero warnings.

## Task 2: Add Installable CI Inspection Helper

**Files:**

- Create: `skills/ci-cd-gate/scripts/inspect-pr-checks.py`
- Test: `python3 skills/ci-cd-gate/scripts/inspect-pr-checks.py --help`

- [ ] **Step 1: Copy the Apache-2.0 source helper into the skill-local script path**

Run:

```bash
mkdir -p skills/ci-cd-gate/scripts
cp /Users/masiyuan/.codex/plugins/cache/openai-curated/github/ed8ce2ea/skills/gh-fix-ci/scripts/inspect_pr_checks.py skills/ci-cd-gate/scripts/inspect-pr-checks.py
```

Expected: `skills/ci-cd-gate/scripts/inspect-pr-checks.py` exists and starts with `#!/usr/bin/env python3`.

- [ ] **Step 2: Rename script description and output fields only where needed**

Apply these exact semantic edits:

- Keep the helper read-only.
- Keep `gh auth status`, PR resolution, `gh pr checks`, run metadata, and log extraction behavior.
- Add stale metadata fields to JSON output when available:
  - `localHeadSha`
  - `remoteHeadSha`
  - `runHeadSha`
  - `isStale`
- Add pending check detection when `state/status` is queued, pending, in_progress, or waiting.
- Keep exit code `0` only when no failing checks are detected.
- Keep exit code `1` when failures exist or when required inspection fails.

The helper must still support:

```bash
python3 skills/ci-cd-gate/scripts/inspect-pr-checks.py --repo "." --pr "123"
python3 skills/ci-cd-gate/scripts/inspect-pr-checks.py --repo "." --pr "123" --json
python3 skills/ci-cd-gate/scripts/inspect-pr-checks.py --repo "." --max-lines 200 --context 40
```

- [ ] **Step 3: Verify help output**

Run:

```bash
python3 skills/ci-cd-gate/scripts/inspect-pr-checks.py --help
```

Expected: exits `0` and includes:

```text
Inspect failing GitHub PR checks
--repo
--pr
--json
```

- [ ] **Step 4: Verify blocked behavior outside a git repo**

Run:

```bash
tmpdir="$(mktemp -d)"
python3 "$PWD/skills/ci-cd-gate/scripts/inspect-pr-checks.py" --repo "$tmpdir" --json
```

Expected: exits non-zero and prints an error equivalent to:

```text
Error: not inside a Git repository.
```

- [ ] **Step 5: Verify install portability**

Run:

```bash
tmpdir="$(mktemp -d)"
./install.sh --target both --dest "$tmpdir"
test -f "$tmpdir/.codex/skills/ci-cd-gate/SKILL.md"
test -f "$tmpdir/.codex/skills/ci-cd-gate/scripts/inspect-pr-checks.py"
test -f "$tmpdir/.codex/skills/ci-cd-gate/references/source.md"
test -f "$tmpdir/.claude/skills/ci-cd-gate/SKILL.md"
test -f "$tmpdir/.claude/skills/ci-cd-gate/scripts/inspect-pr-checks.py"
test -f "$tmpdir/.claude/skills/ci-cd-gate/references/source.md"
```

Expected: all `test -f` commands exit `0`.

## Task 3: Integrate PM Routing And Quality Gate

**Files:**

- Modify: `skills/pm-orchestrator/SKILL.md`
- Modify: `skills/harness-quality-gate/SKILL.md`
- Test: `rg -n "ci-cd-gate|CI/CD Gate|CI/CD" skills/pm-orchestrator/SKILL.md skills/harness-quality-gate/SKILL.md`

- [ ] **Step 1: Update PM routing table**

In `skills/pm-orchestrator/SKILL.md`, update the routing table row:

```markdown
| final release readiness | `/harness-quality-gate` | CI/CD review pack when available |
```

to:

```markdown
| CI/CD status and failure triage | `/ci-cd-gate` | `/tester`, `/review-github-actions`, `/review-security` |
| final release readiness | `/harness-quality-gate` | `/ci-cd-gate` when PR/CI context exists |
```

Expected: PM has an explicit primary skill for CI/CD status and failure triage.

- [ ] **Step 2: Add CI/CD Gate Scheduling section**

Add this section after `Review Pack Scheduling`:

```markdown
### CI/CD Gate Scheduling

PM SHOULD route to `/ci-cd-gate` when:

- user asks whether CI, GitHub Actions, PR checks, build, release, or deploy is ready.
- `/tester` passes locally but remote checks are failing, pending, stale, or unknown.
- final handoff depends on PR checks or release readiness.
- CI failure needs classification before assigning `/developer`, `/tester`, `/review-github-actions`, or `/review-security`.

`/ci-cd-gate` is read-only. Rerun workflow, push fix, post PR comment, merge, release, or deploy remain `external-side-effect` operations and require Operation Gate confirmation.
```

- [ ] **Step 3: Add CI/CD backflow rows**

In the PM failure backflow table, add rows:

```markdown
| CI test/lint/typecheck/build failure | `/ci-cd-gate` classifies, then `/developer` or `/tester` |
| CI workflow/config failure | `/ci-cd-gate`, then `/review-github-actions` and `/developer` |
| CI secret/permission/security failure | `/ci-cd-gate`, then `/review-security` plus `/review-github-actions`, or user decision |
| CI pending or stale | `/ci-cd-gate` returns `PENDING` / `STALE`; PM cannot claim release-ready |
```

Expected: CI failures no longer fall through generic quality gate language.

- [ ] **Step 4: Update harness quality gate**

In `skills/harness-quality-gate/SKILL.md`, add to the minimal execution flow:

```markdown
   - 如果当前分支有关联 PR 或用户要求 release readiness，调用或读取 `/ci-cd-gate` 的 `CI/CD Gate Result`。
```

Add to判定规则:

```markdown
- `BLOCK`：远端 required CI/CD checks 失败、当前 commit 没有对应 CI 结果，或 `/ci-cd-gate` 返回 `BLOCK` / `STALE`。
- `WARN`：`/ci-cd-gate` 返回 `PENDING`，或只有非阻断 external checks 缺少诊断。
```

Expected: quality gate can consume CI/CD evidence without duplicating log triage.

- [ ] **Step 5: Verify routing references**

Run:

```bash
rg -n "ci-cd-gate|CI/CD Gate|CI/CD Gate Scheduling" skills/pm-orchestrator/SKILL.md skills/harness-quality-gate/SKILL.md
```

Expected: output includes PM scheduling, PM backflow, and quality gate references.

## Task 4: Update Docs And Skill Audit Key Set

**Files:**

- Modify: `scripts/checks/skill-standard.mjs`
- Modify: `skills/skill-audit/scripts/skill-standard.mjs`
- Modify: `README.md`
- Modify: `AGENTS.md`
- Test: `node scripts/checks/skill-standard.mjs`

- [ ] **Step 1: Add `ci-cd-gate` to root skill standard key skills**

In `scripts/checks/skill-standard.mjs`, add:

```js
  "ci-cd-gate",
```

inside the `keySkills` set near the other PM/gate skills.

Expected: root check treats `/ci-cd-gate` as a key skill requiring pressure scenario coverage.

- [ ] **Step 2: Add `ci-cd-gate` to skill-audit mirrored key skills**

In `skills/skill-audit/scripts/skill-standard.mjs`, add the same line:

```js
  "ci-cd-gate",
```

Expected: installed `/skill-audit` behavior matches repo-local check behavior.

- [ ] **Step 3: Update README skills list**

In `README.md`, add this row under Workflow Skills near `/harness-quality-gate`:

```markdown
| `/ci-cd-gate` | 读取 GitHub Actions PR checks 和失败日志，判断 CI/CD 是否阻断交付，并把失败回流给 PM 可调度的实现、测试或 review skills |
```

In the "当前能力 / 已经落地" section, update the CI/CD wording to say:

```markdown
- **CI/CD gate foundation**：`/ci-cd-gate` 读取 GitHub Actions PR checks 和失败日志，输出只读 gate result 和 PM backflow；自动 rerun、push、merge、deploy 仍属于后续 operation gate。
```

Expected: README does not claim full CI/CD automation.

- [ ] **Step 4: Update AGENTS skill quick reference**

In `AGENTS.md`, add this row near `/harness-quality-gate`:

```markdown
| `/ci-cd-gate` | GitHub Actions PR checks / CI 日志读取、失败分类、交付阻断判断和 PM 回流建议 |
```

Expected: installed projects expose `/ci-cd-gate` in their quick reference after docs sync or install.

- [ ] **Step 5: Run skill standard check**

Run:

```bash
node scripts/checks/skill-standard.mjs
```

Expected: `errors: 0` and status `PASS`. Historical warnings may remain, but `/ci-cd-gate` should not introduce missing source attribution or pressure scenario warnings.

## Task 5: End-To-End Smoke Test With Temporary Project

**Files:**

- No source file changes expected unless the smoke test exposes a defect.
- Test: temporary install and helper blocked-path behavior.

- [ ] **Step 1: Run install smoke**

Run:

```bash
tmpdir="$(mktemp -d)"
./install.sh --target both --dest "$tmpdir"
find "$tmpdir/.codex/skills/ci-cd-gate" -maxdepth 3 -type f | sort
find "$tmpdir/.claude/skills/ci-cd-gate" -maxdepth 3 -type f | sort
```

Expected: both installed skill directories include:

```text
SKILL.md
references/pressure-scenarios.md
references/source.md
scripts/inspect-pr-checks.py
```

- [ ] **Step 2: Run installed helper help**

Run:

```bash
python3 "$tmpdir/.codex/skills/ci-cd-gate/scripts/inspect-pr-checks.py" --help
python3 "$tmpdir/.claude/skills/ci-cd-gate/scripts/inspect-pr-checks.py" --help
```

Expected: both commands exit `0`.

- [ ] **Step 3: Run installed helper blocked-path check**

Run:

```bash
nongit="$(mktemp -d)"
python3 "$tmpdir/.codex/skills/ci-cd-gate/scripts/inspect-pr-checks.py" --repo "$nongit" --json
```

Expected: exits non-zero and reports that the path is not inside a Git repository.

- [ ] **Step 4: Optional live PR check**

Only run this step if the current repo has an authenticated `gh` session and an associated PR:

```bash
gh auth status
gh pr view --json number,url,headRefOid,headRefName
python3 skills/ci-cd-gate/scripts/inspect-pr-checks.py --repo "." --json
```

Expected when no failing checks exist:

```text
PR #<number>: no failing checks detected.
```

Expected when failures exist: JSON includes `results` with failing check names, run id when available, and failure snippets. Do not require a green remote CI result to complete implementation if no PR exists locally; record `not run: no associated PR` instead.

## Task 6: Final Verification And Handoff

**Files:**

- Modify only if prior tasks reveal a doc or check gap.
- Test: final repo checks.

- [ ] **Step 1: Run repository diff check**

Run:

```bash
git diff --check
```

Expected: no whitespace errors.

- [ ] **Step 2: Run skill standard check**

Run:

```bash
node scripts/checks/skill-standard.mjs
```

Expected: status `PASS`, `errors: 0`. Historical warnings are acceptable only if unchanged from pre-implementation baseline.

- [ ] **Step 3: Run install smoke**

Run:

```bash
tmpdir="$(mktemp -d)"
./install.sh --target both --dest "$tmpdir"
test -f "$tmpdir/.codex/skills/ci-cd-gate/SKILL.md"
test -f "$tmpdir/.codex/skills/ci-cd-gate/scripts/inspect-pr-checks.py"
test -f "$tmpdir/.claude/skills/ci-cd-gate/SKILL.md"
test -f "$tmpdir/.claude/skills/ci-cd-gate/scripts/inspect-pr-checks.py"
```

Expected: all checks pass.

- [ ] **Step 4: Produce final handoff**

Final response must include:

```markdown
### Implementation Summary
- created:
- modified:
- verification:
- not_run:
- remaining_risks:
- recommended_next:
```

Expected: `remaining_risks` explicitly states that v1 does not auto rerun, push, comment, merge, deploy, or diagnose non-GitHub Actions providers deeply.

## Self-Review

### Spec Coverage

- User asked whether third-party libraries/skills can be reused: plan uses local OpenAI `gh-fix-ci` and keeps Composio `pr-review-ci-fix` out of v1.
- User approved first-version direction: plan creates `/ci-cd-gate` with `gh` and PM backflow.
- PM integration covered: Task 3 updates routing, scheduling, backflow, and quality gate.
- Installability covered: Task 2 and Task 5 verify skill-local script and references install to `.codex` and `.claude`.
- Safety covered: skill explicitly forbids rerun, push, comment, merge, deploy without operation gate.

### Placeholder Scan

The plan uses exact file paths, concrete commands, explicit expected results, and bounded implementation steps.

### Type And Contract Consistency

The output contract consistently uses `CI/CD Gate Result`, `capability: ci_cd_gate`, and status values `PASS / WARN / BLOCK / PENDING / STALE / BLOCKED`. PM and quality gate references should use the same skill name `/ci-cd-gate`.
