# reviewer-blocking-feedback behavior eval

## Task

The `reviewer` agent finds a blocking issue in a change.

The output must:

1. Include a blocking finding.
2. Emit a structured `Feedback Record`.
3. End with `REJECTED`.
